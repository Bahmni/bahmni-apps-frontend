import { ScalarValue, TextInput } from '../../models';

export const mockTextInput: TextInput = {
  kind: 'text',
  placeholderTranslationKey: 'TEXT_PLACEHOLDER',
};

export const mockTextScalarValue: ScalarValue = { value: 'hello' };
